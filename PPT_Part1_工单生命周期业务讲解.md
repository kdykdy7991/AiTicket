# PPT 第一部分内容：一张工单是如何从产生到处理完成的

> 本文档完全基于当前代码（backend/app/、frontend/src/、需求文档）梳理，不引入代码中未实现的能力。
> 所有"代码中未确认"之处已明确标注。

---

## 1. 工单涉及的主要角色

> 角色来源：用户表 `users.role` 字段取值 + `UserSkillGroup.is_dispatcher` 标记 + 前端 `authStore.isAdmin / isAgent` 的判定逻辑。

| 代码中的角色 | 通俗叫法 | 谁来用 | 能做什么 |
|---|---|---|---|
| **admin** | 系统管理员 | 公司 IT / 系统配置员 | 所有权限，管理用户、对接部门、SLA 策略 |
| **agent**（客服） | 客服坐席（含客服组长 `is_group_leader=true`） | 客服团队一线人员 | 接听电话录入工单、查重、追加信息、催办、回访、归档、退回 |
| **handler**（处理人/技术人员） | 内部技术、市场、售后处理人员 | 各对接部门（如"技术组""市场组""售后组"） | 处理自己被指派的工单，提交处理说明、申请暂缓、退回上一状态 |
| **部门对接人 dispatcher**（不是独立角色，而是 handler 上加的标记 `UserSkillGroup.is_dispatcher=true`） | 对接部门负责人 | 每个对接部门通常 1～N 名 | 看到派到自己部门的待受理工单、把工单指派给具体处理人 |

> **特别注意：客户/报修人不登录系统**
> 代码中没有"客户账号""客户登录"功能（需求文档 §五明确"客户渠道接入"是 **二期规划，本次未实现**）。客户只是通过电话/微信/Web/APP/邮件等渠道联系客服，**由客服把客户信息录入系统**，这张工单才算"产生"。所以角色只有 4 类，全部是公司内部人员。

---

## 2. 一张工单的完整生命周期

### 2.1 生命周期总览（基于 state_machine.py 的 7 个状态 + 实际权限）

代码 `app/services/state_machine.py` 定义了 7 个状态：
- `pending` 待受理（初始）
- `open` 处理中
- `resolved` 已处理
- `on_hold` 暂缓处理
- `returned` 已退回
- `archived` 已归档（终态）
- `cancelled` 已撤销（终态）

### 2.2 真实业务流程（按时间顺序）

| 步骤 | 谁 | 做了什么 | 工单状态变化 | 系统是否留痕 |
|---|---|---|---|---|
| ① 接听客户来电 | 客服坐席 | 接电话，听客户描述问题 | 无（还没建单） | 无（电话里说的暂未入系统） |
| ② 录入工单 | 客服坐席 | 填写：客户手机号、单位名称、设备 SN、来电号码类型、所属区域、问题详情；选择一级分类/二级分类、对接部门（如"技术组"）、部门对接人、优先级（P1～P4）、工单来源（电话/微信/Web/App/邮件）；可"暂存草稿"或"提交工单" | **新建 → pending（待受理）** | 生成工单编号（`YYYYMMDD-XXXX`），写入 `TicketStateLog` 一条"→待受理"日志 |
| ③ 系统自动查重 | 系统 | 后端自动查同一手机号、相同二级分类下 5 条未完结的同类工单，回传"重复工单提醒" | 不变 | 在创建工单的返回值里返回 `warnings.duplicate_detected`，供前端提示 |
| ④ 系统自动算 SLA 时限 | 系统 | 根据"对接部门 + 优先级"在 `sla_policies` 表里匹配策略，自动算出 `solution_deadline`（解决时限） | 不变 | `Ticket.solution_deadline` 字段写入 |
| ⑤ 部门对接人看到工单 | 部门对接人（dispatcher） | 登录后，在工单列表里看到这张工单出现在自己的待受理工单池（权限过滤：dispatcher_id = 自己 或 owner_id = 自己） | 不变 | 无（仅查询） |
| ⑥ 指派处理人 | 部门对接人 | 在工单详情里选具体处理人，点"确认指派"。系统自动把状态改成处理中 | **pending → open** | 写入 `TicketStateLog`：`from=pending, to=open, operator=对接人`，记录首次处理人 `first_owner` |
| ⑦ 处理人处理 | 处理人（owner） | 查看问题详情，必要时通过"追加信息"补充材料（客服也可追加），需要时联系客户（电话沟通，但**通话本身不入系统**），处理完成后提交"处理说明" | 仍为 `open` | 处理人提交处理说明时：自动生成一条 `open→resolved` 的 `TicketStateLog`，并保存一条 `Article(type=reply, state_key=该日志ID)`，可附带图片附件 |
| ⑧ （可选）申请暂缓 | 处理人 | 工单进行中如果遇到阻塞（如等客户回电、等配件），提交"暂缓"+ 填暂缓时间 | **open → on_hold** | 写状态日志，写 `hold_until` |
| ⑨ （可选）从暂缓恢复 | 处理人 | 暂缓原因消除后，点"确认"继续处理 | **on_hold → open / resolved** | 写状态日志 |
| ⑩ 客服回访 | 客服（agent / admin） | 处理人提交"已处理"后，客服电话回访客户确认是否真的解决了 | **resolved（已处理）**，等客服动作 | 这步目前**没有强制要求**，系统只是把状态停在 `resolved` 等客服动作 |
| ⑪ 归档 / 退回 | 客服（agent / admin） | 在详情页选：① 已回访（满意/一般/不满意/未评价），点"归档" → 写归档说明、满意度；② 或无需回访，点"归档"；③ 或确认问题没解决，点"退回"→ 选原因退回处理人重做 | resolved → **archived**（归档/终态），或 resolved → **open / pending**（退回重做） | 归档：写终态日志 + `archive_notes`、`is_callbacked`、`satisfaction`、`closed_at`；退回：写状态日志 `from=resolved, to=open 或 pending` |
| ⑫ （可选）撤销 | 创建该工单的客服 | 在任何非终态环节（pending/open/on_hold/returned）点"撤销" + 填原因 | **→ cancelled（终态）** | 写终态日志、`closed_at` |
| ⑬ 终态后 | — | 工单进入 `archived` 或 `cancelled` 后，**不能再追加任何沟通记录**，列表里以灰色呈现 | 终态不可流转 | 终态操作人快照写入 `archived_by` / `cancelled_by` |

### 2.3 中间存在的"工作产物"

| 产物 | 存在形式 | 谁能看到 |
|---|---|---|
| 工单正文 | `Ticket.description` + `Ticket.symptom` + 客户信息字段 | 列表 / 详情 |
| 状态流转记录 | `TicketStateLog` 表，每条记录：哪个时间、由谁、从哪个状态到哪个状态、停留了多少分钟、原因是什么 | 详情页"流转时间线" |
| 沟通记录 | `Article` 表，三种类型：<br>• `reply` 处理说明（只有当前处理人可写，提交后状态自动转 resolved）<br>• `addition` 追加信息（客服/管理员可写，需填"追加原因"，可带图片附件）<br>• `reminder` 催办（客服可写，限一次，≤200 字） | 详情页"沟通时间线" |
| 附件 | `ArticleAttachment` 表，只允许**图片**（单张 ≤ `UPLOAD_MAX_SIZE`），仅在 `addition` 类型下可上传 | 详情页 |
| 退回记录 | 通过 `TicketStateLog` + 工单上 `has_returned` 布尔字段标记 | 详情页 |
| 催办记录 | `Ticket.urged_at` 时间 + `urged_by_id` + 一条 `Article(type=reminder)` | 详情页 + 列表置顶 |
| SLA 超时 | 后台 `sla_scanner.py` 每分钟扫描一次；超时后 `sla_solution_breached=true`；列表把超时工单自动置顶 | 列表/详情 |
| 审计日志 | `AuditLog` 表，记录"谁在何时改了哪个资源的什么字段"（后端有，前端未展示）| 后端日志 |

### 2.4 哪些人有权做哪些动作（权限矩阵 · 严格依据代码）

> 来源：`app/api/v1/tickets.py:242` 的 `check_transition_permission` 与 `app/api/v1/articles.py` 的 `ADDITION_ALLOWED_ROLES / REPLY_ALLOWED_STATES` 等定义。

| 动作 | pending | open | on_hold | resolved | returned |
|---|---|---|---|---|---|
| **分派（→open）** | 部门对接人 / admin | — | — | — | — |
| **处理说明（→resolved）** | — | 处理人 owner | 处理人 owner | — | — |
| **暂缓（→on_hold）** | — | 处理人 owner | — | — | — |
| **退回处理人重做（→open）** | — | 处理人 owner（把自己退回待受理）| 处理人 owner | 创建者（客服）或客服团队 | — |
| **退回创建者（→returned）** | 部门对接人 / admin | — | — | — | — |
| **重新提交（→pending）** | — | — | — | — | 创建者 / admin |
| **归档（→archived）** | — | — | — | 客服团队（agent） / admin | — |
| **撤销（→cancelled）** | 创建者（发起人）/ admin | 创建者 / admin | 创建者 / admin | — | 创建者 / admin |
| **追加信息** | 客服（agent）/ admin | 客服 / admin | 客服 / admin | — | 客服 / admin |
| **催办**（一生一次） | 客服 / admin | 客服 / admin | 客服 / admin | — | — |

---

## 3. 一个适合 PPT 展示的典型工单案例

> 案例完全基于现有能力设计，没有引入新功能。

### 案例："某政企客户来电反映内部管理系统登录不上"

**背景**
- 客户类型：政企（系统代码中 `customer_type=enterprise` 是当前唯一开启入口，`personal` 在前端已临时关闭）
- 来电渠道：电话（系统 `channel=phone`，也是默认值）
- 客户单位：星河科技有限公司（虚构）
- 联系电话：13912345678
- 问题：自 9 月 3 日下午起，5 名员工反馈无法登录公司内部"业务中台"系统，提示"账号不存在"。但账号在 AD 里确实存在。

---

### 时间轴剧本（按真实业务顺序）

**T = 0 分钟｜客户来电**
> 角色：客户（不进系统） → 客服坐席 小张

客户拨打 400 客服热线，描述上述问题。客服小张接听，在系统里点"**新建工单**"，依次填：

- 来电号码：13912345678（手机），号码类型"手机"
- 联系人：张主任
- 联系号码：13987654321
- 单位名称：星河科技有限公司（政企必填）
- 设备 SN：留空
- 所属区域：北京-海淀
- 问题详情："9 月 3 日下午起，5 名员工反馈无法登录'业务中台'，提示账号不存在，AD 里账号正常。"
- 一级分类：业务系统
- 二级分类：业务中台 / 登录失败
- **对接部门**：技术组
- **部门对接人**：技术组的 李工（dispatcher）
- 优先级：P3 较大事件
- 工单来源：电话

提交后系统生成工单编号 `20250903-0001`，状态 **pending（待受理）**，并自动计算出解决时限。

**T = 0 ～ 5 分钟｜系统自动查重与提醒**
系统检测到该手机号最近 14 天内已有 2 张同类（"业务中台/登录失败"）未关闭工单，在前端弹窗"疑似重复工单"，小张判断这是新问题（5 名员工是新发生的），继续提交并勾选"非重复"。

**T = 5 分钟｜部门对接人看到工单**
> 角色：部门对接人 李工

李工登录系统，"工单列表"里出现 20250903-0001 在自己的待受理池（因 `dispatcher_id=李工`）。李工点开详情，看到完整客户信息和问题描述。

**T = 10 分钟｜指派处理人**
> 角色：部门对接人 李工

李工判断问题应该交给后端开发组的 王工，在"指派处理人"卡片里选王工，点"确认指派"。
- 状态：**pending → open（处理中）**
- 系统记录 `first_owner=王工`，后续改派也会保留这条记录
- 王工在自己的待办里看到这张工单

**T = 30 分钟｜处理人补充信息**
> 角色：客服 小张（追加信息权限）

小张在与客户进一步电话沟通后得知"昨天升级过 AD 域控补丁"。小张打开工单详情，在"追加信息"里写了：
> 追加原因：客户补充关键信息
> 追加内容：客户昨天下午对 AD 域控打过 KB5034123 补丁，怀疑与登录失败相关。建议王工优先排查域控侧。
（可附一张客户截屏的图片附件）

**T = 1 小时｜处理人申请暂缓**
> 角色：处理人 王工

王工排查后发现需要等运维侧重启域控服务（业务时段不可操作），于是点"暂缓"，填暂缓时间 = 次日 09:00。
- 状态：**open → on_hold（暂缓处理）**

**T = 次日 09:30｜处理人恢复处理并提交处理说明**
> 角色：处理人 王工

王工验证后确认：KB5034123 补丁导致 AD 认证服务对老版本 LDAP 查询失败，已协调运维回滚补丁，5 名员工全部恢复登录。
- 王工在工单详情里提交"处理说明"：详细写清根因 + 处理动作 + 验证结果
- 提交后系统自动：**open → resolved（已处理）**，并记录 `solved_at`，如果超过 `solution_deadline` 自动标记 `sla_solution_breached=true`

**T = 次日 10:00｜客服回访**
> 角色：客服 小张

小张电话回访张主任，张主任回复"问题确实解决了，没问题了"。
- 小张在详情页选"已回访" → 满意度："满意"
- 点"归档"
- 状态：**resolved → archived（已归档）**
- 写入 `archive_notes`（如："客户确认已解决"）、`satisfaction=satisfied`、`closed_at`、`closed_duration_minutes=约 1440 分钟`

至此这张工单完整闭环。

---

## 4. 目前工单系统中已经沉淀了哪些数据

> 数据字段全部来自 `backend/app/models/ticket.py`、`user.py`、`sla.py`、前端 `frontend/src/types/index.ts` 以及导出 CSV 列定义。

### 4.1 一张工单"基本信息"沉淀

| 数据 | 字段名（代码） | 在 UI 里长什么样 |
|---|---|---|
| 工单编号 | `Ticket.number`（如 20250903-0001） | 列表 / 详情顶部 |
| 工单标题/描述 | `Ticket.title` / `description` / `symptom`（问题详情） | 详情顶部"详细描述" |
| 状态 | `Ticket.state`（7 种）+ 中文标签 | 状态标签 + 时间线 |
| 优先级 | `Ticket.priority`（p1_urgent / p2_high / p3_normal / p4_enterprise） | 列表颜色 + 详情 |
| 来源渠道 | `Ticket.channel`（phone / wechat / web / app / email） | 列表 / 详情 |
| 客户类型 | `Ticket.customer_type`（personal / enterprise） | 详情 |
| 客户姓名 | `Ticket.customer_name` | 详情 / 导出 |
| 客户单位 | `Ticket.customer_company` | 详情 / 导出 |
| 来电号码 + 类型 | `customer_phone` + `customer_phone_type`（mobile / landline） | 详情 / 导出 |
| 联系号码 / 联系人 | `contact_phone` / `customer_name`（作为联系人） | 详情 |
| 设备编号 | `Ticket.device_sn` | 详情 / 导出 |
| 所属区域 | `region_id` + `region_name`（省/市/区三级 Region 表） | 详情 |
| 客户等级 | `customer_level`（默认 normal） | 详情 |
| 一级/二级分类 | `TicketCategory` 两级树，`category_id` 指向叶子 | 详情 / 列表 |
| 对接部门（技能组） | `skill_group_id` → `skill_groups.name` | 详情 / 列表 |
| 部门对接人 | `dispatcher_id` → `users.name`（即创建工单时选的那个 dispatcher） | 详情 / 列表 / 导出"实际对接人" |
| 处理人 | `owner_id` → `users.name`（指派后才有） | 详情 / 列表 / 导出"实际处理人" |
| 首次处理人 | `first_owner_id`（保留历史，即便转派也保留） | 详情 |
| 创建人 | `creator_id` → `users.name`（录单的客服） | 详情 / 导出"工单创建人" |
| 创建时间 | `created_at` | 列表 / 详情 / 导出 |
| 更新时间 | `updated_at` | 列表 |
| 解决时间 | `solved_at` | 详情 / 报表 |
| 关闭时间 | `closed_at`（归档/撤销时写入） | 详情 / 报表 / 导出"结束时间" |
| 关闭总耗时 | `closed_duration_minutes`（分钟） | 报表 / 导出 |

### 4.2 一张工单"处理过程"沉淀

| 数据 | 字段 | 说明 |
|---|---|---|
| 是否重复工单 | `is_duplicate` + `duplicate_reason` | 创建时如果勾选"重复工单" |
| 关联的重复工单 | `linked_ticket_id` | 指向被认为是重复的那张 |
| 是否升级 | `is_escalated` | 标记升级 |
| 是否退回过 | `has_returned` | 标记工单历史上发生过退回动作 |
| 退回接收人快照 | `returned_to_user_id`（pending→returned 时分配给创建人） | 详情 |
| 暂缓到期时间 | `hold_until` | 暂缓时填的时间 |
| 最终解决方案 | `resolution`（Text） | 列表 / 详情 |
| 是否已解决 | `resolved`（Boolean） | 详情 |
| 归档说明 | `archive_notes` | 详情 / 导出 |
| 是否需要回访 | `callback_required`（默认 true） | 详情 |
| 是否已回访 | `is_callbacked` | 列表 / 详情 / 导出 |
| 客户满意度 | `satisfaction`（satisfied/average/dissatisfied/unrated） | 详情 / 报表 |

### 4.3 一张工单"SLA / 时效"沉淀

| 数据 | 字段 | 说明 |
|---|---|---|
| 解决时限（截止时间） | `solution_deadline`（创建时按优先级+部门查 sla_policies 自动算） | 列表倒计时 |
| 首次响应时限 | `first_response_deadline`（字段保留，当前版本未自动计算） | 报表占位 |
| 实际首次响应时间 | `first_response_at`（字段保留，未自动写入） | 报表占位 |
| 解决是否超时 | `sla_solution_breached`（后台 sla_scanner.py 每分钟扫描标记） | 列表置顶/标红 |
| 首次响应是否超时 | `sla_first_response_breached`（字段保留） | 报表占位 |

### 4.4 一张工单"沟通历史"沉淀（独立 Article 表）

每条记录包括：
- `type`：`reply` 处理说明 / `addition` 追加信息 / `reminder` 催办
- `sender_id`：谁发的（必须是系统用户：客服/处理人/管理员）
- `body`：沟通正文
- `subject`：可选主题
- `state_key`：关联到哪一条状态流转日志（处理说明专用）
- `append_reason`：追加原因（仅 addition）
- `created_at` / `updated_at`
- 关联的 `ArticleAttachment`（图片附件）：`original_filename`、`content_type`、`size`、`storage_path`

### 4.5 一张工单"状态流转时间线"沉淀（独立 TicketStateLog 表）

每条记录包括：
- `from_state` → `to_state`：从哪个状态到哪个状态
- `operator_id`：谁触发的这次流转
- `reason`：原因（退回/暂缓/撤销时必填）
- `duration_minutes`：在上一状态停留了多少分钟（自动算）
- `created_at`：时间戳
- **人员快照**（这是这张表的关键特色）：
  - `creator_id_snapshot`：流转发生那一刻的工单创建者
  - `dispatcher_id_snapshot`：流转发生那一刻的部门对接人
  - `owner_id_snapshot`：流转发生那一刻的处理人
  - 这样做是为了让历史时间线即使在工单后续转派、退回之后，依然能看到"那一刻是谁"

### 4.6 其他"边角"沉淀

| 数据 | 字段 / 表 |
|---|---|
| 钉钉通知发送历史 | `Dingtalk_notifications` 表（保存尝试发送的钉钉消息、目标 webhook、成功/失败状态）—— **代码中已留表，但实际未启用钉钉发送**（`services/notification.py` 是 placeholder） |
| 催办相关 | `Ticket.urged_at` + `urged_by_id` + 一条 `Article(reminder)` |
| 草稿 | `Ticket.is_draft=true` 的工单 |
| 终态操作人 | `archived_by_id` / `cancelled_by_id`（来自 state_logs 最近一次 to_state∈{archived,cancelled}） |
| 审计日志 | `audit_logs` 表（`action`/`resource_type`/`changes`JSONB/`ip_address`）—— 后端写入，前端未呈现 |

---

## 5. 当前流程中天然存在的"人工信息工作"

> 这里不是批评系统，而是为后续 AI 接入识别机会点。

### 5.1 哪些步骤需要"人工阅读大量文字"

| 步骤 | 现状 | 工作量 |
|---|---|---|
| 客服接电话录入 | 客户口述 → 客服手敲描述 | 通话可能 5～20 分钟，描述打字 200～1000 字 |
| 处理人理解问题 | 打开详情看 description + symptom + 所有 Article 历史 | 工单越复杂，沟通记录越长，阅读成本越高 |
| 客服确认归档 / 退回 | 要回看整个对话历史判断"是不是真的解决了" | 解决不好很容易被客户投诉"还没好就关了" |
| 客服处理"已退回"工单 | 需要看一遍退回原因 + 之前所有沟通 + 决定是否重新派 | 容易漏掉关键背景 |

### 5.2 哪些步骤依赖"人工理解"问题

- 客户口述"我登不上去"，客服必须听懂"是浏览器问题、密码问题、还是后端问题"，然后选对二级分类
- 客服必须判断"该不该标注为重复工单"——目前只是按"手机号+分类"机械查重，**真正"是不是同一个问题"需要人判断**
- 处理人必须判断"今天能不能修"，决定是否暂缓；"是不是真修好了"决定能否回访
- 部门对接人必须判断"这张工单该派给技术组哪个具体的人"——**目前完全凭经验和记忆**，没有历史匹配建议

### 5.3 哪些步骤需要"人工查历史记录"

- 新客户来电时，客服手动翻"该手机号最近 14 天历史工单"
- 处理人开工单时，要手动看"以前这个客户的同类问题是怎么解决的"
- 客服做回访时，要手动翻完整个 Article 时间线
- 客服/管理员做月度报表时，要手动汇总 `by_category` / `avg_resolution_per_group` 等

### 5.4 哪些步骤需要"凭经验判断该分派给谁"

- 部门对接人指派：今天 5 个处理人在不在岗？擅长哪类问题？历史接过多少？当前手上有几张？**系统目前只显示"对接部门下所有启用的用户"**（`/skill-groups/{id}/handlers`），没有任何智能推荐。
- 优先级评估：P1～P4 全靠客服主观判断，**系统没有任何辅助**（需求文档 §五 提到"自动分流"是二期）

### 5.5 哪些步骤产生了大量历史数据，但关闭后很少再次被利用

- 处理说明（`Article.reply`）：工单一关闭，就静静躺在数据库里。**没有任何"知识库"功能去抽取、聚类、检索**。
- 追加信息（`Article.addition`）：尤其是客户的"关键补充信息"，关闭后无人再翻。
- 暂缓原因（`TicketStateLog.reason`）：是宝贵的"什么情况下会卡住"的真实案例，但只有当时的人才看过。
- 退回原因（`TicketStateLog.reason`）：是"什么样的处理结果不合格"的真实反馈，但关闭后无人分析。
- 解决方案（`Ticket.resolution`）：**最有价值的资产**，但目前只能按工单号单条查询，不能跨工单搜"上次这类问题怎么解决的"。

### 5.6 哪些通知和跟进需要"人工主动关注"

| 跟进事项 | 现状 | 痛点 |
|---|---|---|
| 谁来接单 | 部门对接人**每天必须打开列表看**有没有待受理工单 | 没有主动推送 |
| 处理人超时提醒 | 系统会把超时工单置顶标红，但**人是看到列表才知道** | 钉钉通知代码已留 placeholder，**未真正接入** |
| 客户回访 | 客服必须每天扫"已处理+需要回访+未回访"这一类 | 没有"今天该回访哪些"清单 |
| 催办 | **当前实现里客服点了催办后，只是写一条 article 和改 urged_at，并没有真正给处理人发消息**（`remind_handler()` 当前只打日志） | 处理人未必知道 |
| SLA 超时 | 后台只更新 `sla_solution_breached=true`，**不会自动通知** | 钉钉通知代码未接入 |
| 退回 / 撤销原因 | 创建人或对接人需要自己回来查看 | 没有"我退回去的工单现在到哪了"提醒 |

---

## 6. 适合 PPT 的图形设计建议

> 三张图全部面向非技术领导，不用任何技术词汇。

---

### A. 工单完整生命周期图

**目标**：让领导 10 秒钟看懂一张工单会经过哪几个阶段、谁是关键角色。

**结构建议（左右两条轨道 + 圆圈 + 箭头）**：

```
[客户来电]                                                
     │                                                    
     ▼                                                    
① 客服录入  ─────────►  [ 待受理 ]   ◄── 部门对接人    
     │                       │              出现            
     │                       ▼                           
     │                  ② 指派处理人                      
     │                       │                           
     │                       ▼                           
     │              ────►[ 处理中 ] ◄── 技术处理人       
     │                       │              在处理         
     │            ┌──────────┼──────────┐                
     │            ▼          ▼          ▼                
     │         ③ 暂缓     ④ 处理说明   ⑤ 退回(重做)      
     │            │          │          │                
     │            ▼          ▼          ▼                
     │       [ 暂缓 ]   [ 已处理 ] ◄─────┘                
     │                       │                           
     │                       ▼                           
     │              ⑥ 客服回访 + 归档/退回               
     │                       │                           
     │                       ▼                           
     │                [ 已归档/已撤销 ]（终态）          
```

**图上要标注的关键信息**：

1. 顶端用一个"📞 来电"图标代表客户来电（不进系统）
2. 左侧轨道从左到右依次标号 ① ~ ⑥，对应"客户来电→客服录入→部门对接人指派→技术处理人处理→处理人提交说明→客服回访归档"6 个动作
3. 每个动作用大圆圈+大字号中文短语，比如：
   - "客服把问题录入系统"
   - "对接人指派给具体处理人"
   - "处理人处理问题"
   - "处理人写处理说明"
   - "客服回访并归档"
4. 状态用方形框列在右侧：**待受理 → 处理中 → 已处理 → 已归档**，每个状态用不同底色
5. 把"暂缓""退回""撤销"用虚线箭头单独画在主流程下方，并标注"可选/异常路径"
6. **左侧顶部用一个"客户/外部"小人物图标，右侧用"客服 / 对接人 / 处理人"三个人物图标**——让领导一眼看清"谁是内部人员"
7. 不要画"数据库/接口/前后端"——绝对不要出现技术词

**配色建议**：
- 客户相关：浅灰
- 客服动作：蓝色
- 对接人动作：紫色
- 处理人动作：橙色
- 终态：绿色（已归档）/ 红色（已撤销）

---

### B. 一个具体工单的流转故事图

**目标**：用一个真实小案例（政企客户登录失败）让领导"看图就像听故事"。

**结构建议（时间轴从上到下 + 左右两侧角色头像）**：

```
   客户侧              客服小张          部门对接人李工       技术处理人王工        系统自动
     │                    │                  │                  │                 │
     │ 09/03 14:00        │                  │                  │                 │
     │──────── 拨打400 ──►│                  │                  │                 │
     │  "登录不上去"      │ 09/03 14:05      │                  │                 │
     │                    │ 录入信息(1分钟)  │                  │                 │
     │                    │──── 生成工单 ─────────────────────────────────────►  │
     │                    │                 09/03 14:10        │                 │
     │                    │                 ▼ 看到工单          │                 │
     │                    │                 指派给王工(2分钟)   │                 │
     │                    │                 ─────────────────►│                  │
     │                    │                                    │ 09/03 14:30     │
     │                    │                                    │ 排查 + 追加信息 │
     │                    │                                    │ 等运维重启       │
     │                    │                                    │── 申请暂缓 ────►│
     │                    │                                    │                 │
     │                    │                                    │ 09/04 09:30     │
     │                    │                                    │ 回滚补丁 + 验证  │
     │                    │                                    │ 写处理说明(完成) │
     │                    │◄── 09/04 10:00 客服回访 ───────────────────────────│
     │  "问题解决了"      │ 归档 + 满意                       │                 │
     │                    │ 结束(2天)                         │                 │
     ▼                    ▼                  ▼                  ▼                 ▼
```

**关键设计要点**：

1. **顶部是 5 个泳道（角色），底部时间线从左到右**：时间严格递增。
2. 每条箭头代表"一次动作"，箭头要带文字，比如"录入信息""看到工单""指派给王工""申请暂缓""写处理说明""客服回访""归档"。
3. **关键节点用大字号标在时间线上**：9/03 14:05 建单 → 14:10 指派 → 09/04 09:30 处理完成 → 10:00 归档。
4. 总耗时要在图上直接标出："**闭环耗时约 20 小时**"，让领导有量级感。
5. 顶部用一个真实小图：**客户手机号、单位、问题描述**截屏贴在图的左上角（"原始问题：5 名员工登不上业务中台"）。
6. 底部用色块标记工单状态变化："待受理"→"处理中"→"暂缓"→"处理中"→"已处理"→"已归档"。
7. 故事结尾用一句话总结："**所有对话、所有动作、所有时间，全部自动留在了系统里**"——为后面"AI 把工单记录转成知识"做铺垫。

---

### C. 工单中沉淀的信息图

**目标**：让领导看清"工单关闭后，到底给公司留下了什么资产"——为后续 AI 接入铺垫。

**结构建议（一棵"信息树"或"信息冰山"图）**：

**方案 1：信息冰山图**

```
   ┌──────────────────────────────────────┐
   │        水面以上：领导一眼能看到的        │
   │                                          │
   │    📋 工单编号                             │
   │    📞 客户手机号                           │
   │    🏢 客户单位                             │
   │    ✅ 是否已解决 / 满意度                  │
   │                                          │
   └──────────────────────────────────────┘
   ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─
   ┌──────────────────────────────────────┐
   │        水面以下：很少被再次利用的          │
   │                                          │
   │   💬 客服与客户的每一通电话内容            │
   │   📝 处理人写的"处理说明"                 │
   │   ➕ 关键"追加信息"（客户补充的）         │
   │   ⏸  暂缓原因（什么情况下卡住）          │
   │   ↩  退回原因（什么结果被判定不合格）     │
   │   🛠 最终解决方案（最有价值！）           │
   │   📊 全程时间线（多久响应、多久解决）     │
   │   🔁 处理人变更记录                       │
   │   ⏱ SLA 是否超时                         │
   │                                          │
   └──────────────────────────────────────┘
```

**方案 2：信息拼盘图（更适合放在 PPT 单页）**

把一张已关闭工单里"沉淀下来的所有信息"画成 8 个色块拼成一张"信息卡片"：

1. **基础卡片**（公司名/手机号/编号/创建人/创建时间/关闭时间/总耗时）
2. **问题卡片**（问题描述、故障现象、重复标记）
3. **分类卡片**（一级/二级分类、优先级）
4. **人员卡片**（部门对接人、处理人、首次处理人、转派历史）
5. **处理过程卡片**（完整时间线：每一步谁在什么时候做了什么）
6. **沟通卡片**（所有处理说明、追加信息、催办内容）
7. **结果卡片**（最终解决方案、是否解决、客户满意度、归档说明）
8. **效率卡片**（SLA 是否达标、解决时长、超时节点）

**图上要写出的关键文字**：

- 标题："**一张工单结束 ≠ 信息消失**"
- 大字注释："**过去 N 个月，系统中已经沉淀了 X 张工单，但 80% 以上的'处理说明 / 退回原因 / 暂缓原因'几乎没被任何人再翻过**"
- 右下角用一个"🤖 AI 接入点"小标签，提示"这些沉淀数据正是 AI 知识库的最佳素材"

**为后续"AI 把工单记录转成知识"做铺垫的具体画面**：

把"水面以下"那部分用一个图标表示"未被开发的价值"，并标注：
> 这些文字里藏着大量"这家公司的真实故障规律""客户最常问什么""哪种处理最有效"，**只要让 AI 自动读、自动总结、自动检索，客服和处理人的效率会大幅提升**。

---

## 附：本次梳理的"代码中未确认"事项（明确列出，避免脑补）

1. **客户自助提交**：当前**没有**面向客户的前端入口（明确标注"二期"）。
2. **钉钉通知**：模型表 `DingtalkNotification` 已建立，但 `services/notification.py` 只打日志，**未真正发送钉钉**。所以"通知到处理人"目前**只在系统内体现**（催办标志 urged_at + 一条 article），处理人不会收到外部消息。
3. **首次响应时限**：`first_response_deadline` / `first_response_at` / `sla_first_response_breached` 字段在数据库里保留，但代码中**没有自动计算和扫描**。目前 SLA 只跟踪"解决时限"。
4. **暂缓期间的 SLA 计时**：`sla_scanner.py` 的注释明确写 "TODO: on_hold 暂不排除，超时仍会标记"，即**暂缓期间时钟仍在走**。
5. **审计日志前端**：后端 `AuditLog` 表在写入，但前端**没有展示**审计日志页面。
6. **月度报表中的"一次解决率"**：`stats/dashboard` 返回 `first_contact_resolution_rate=0.0`，注释为 TODO，**暂未实现计算逻辑**。
7. **回访满意度字段只有 4 档枚举**（satisfied / average / dissatisfied / unrated），没有开放式评分。

---

## 结语（写给 PPT 演讲者）

- 这套系统的**核心亮点**：把"客户问题"完整结构化（客户信息 + 分类 + 优先级 + 对接部门 + 处理过程 + 时间线 + 结果）。
- 这套系统的**现状短板**：
  1. 所有"判断"都靠人（重复判断、分派判断、优先级判断）
  2. 所有"通知"都靠人自己看（钉钉通道未接入）
  3. 所有"沉淀"都躺在数据库里，未被二次利用
- 这正好就是 **AI 可以介入的三个切入点**，可在 PPT 第一部分结尾引出"为什么我们后面要做 AI"。