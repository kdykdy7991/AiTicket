-- ============================================================
-- 下线「已回访」状态：删除所有已回访工单
-- 关联的 articles / state_logs / reminders 会级联删除
-- ============================================================

DELETE FROM tickets WHERE state = 'callbacked';
